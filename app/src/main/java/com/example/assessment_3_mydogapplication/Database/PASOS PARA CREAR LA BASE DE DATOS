PASOS PARA CREAR UNA BASE DE DATOS

1 PRIMERO SE CREO LA CARPETA DATABASE

2 SE CREO UNA CARPETA LLAMADA DbModel con el modelo de la base de datos

3 se creo el dao que es la que tiene la info para la base de datos

4 se creo la base de datos (BreedDatabase)

5 Se abre el service locator que tiene actua como el orquestador del proyecto y se unicializa la base de datos

6 luego de inicializado, se debe ir a la carpeta utilidades y configurarlo

7 ahora se modifica el repositorio para que se pueda usar la base de datos

8 luego se modifica el lugar donde se tiene la data ordenada, es importante tener en cuenta que esto se debe hacer
  si o si puesto que es lo que alimentara la base de datos, al mismo tiempo hay que tener en cuenta que el lugar en el que se
  configura esta parte es en el lugar en donde la data son OBJETOS, en el video del profesor, el lo pudo hacer directamente
  desde el modelo web, en este proyecto como la lista es un mapeo, la api no entrega objetos ya listos para implementar
  en su lugar lo que se hace es crear una clase en este caso Breeds que transforma es data en objetos legibles para el codigo
  por ende, es ahi en donde se debe colocar el codigo que comunica la base de datos

9 volvemos al repositorio para terminar de configurar la base de datos ahi

10 ya con esto, la base de datos se ha implementado, y para corroborar el funcionamiento, simplemente se ejecuta la aplicacion en el
   emulador, luego el emulador se pone en modo avion y se cierra la app, al momento de abrirla nuevamente debe aparecer data.



